<RelativeLayout xmlns:android="http://schemas.android.com/apk/res/android"
    android:id="@+id/mainview"
    android:layout_width="fill_parent"
    android:layout_height="fill_parent"
    android:background="@android:color/white"
    >


    <RelativeLayout
        android:id="@+id/movoBluetoothToggle"
        android:layout_width="fill_parent"
        android:layout_height="wrap_content"
        android:layout_centerInParent="true"
        android:visibility="visible"
        >
        <TextView
            android:id="@+id/movoText"
            android:layout_width="wrap_content"
            android:layout_height="wrap_content"
            android:layout_alignParentTop="true"
            android:layout_centerHorizontal="true"
            android:gravity="center"
            android:textSize="20sp"
            android:text="Turn Wave Bluetooth OFF:\nHold the button for 3 seconds until a red light flashes."/>
        <ImageView
            android:id="@+id/movoButton"
            android:layout_centerHorizontal="true"
            android:layout_below="@+id/movoText"
            android:layout_width="140dip"
            android:layout_height="200dip"
            android:src="@drawable/first_sync_4"/>

    </RelativeLayout>
    <RelativeLayout
        android:id="@+id/movoBluetoothFailed"
        android:layout_width="fill_parent"
        android:layout_height="wrap_content"
        android:layout_centerInParent="true"
        android:visibility="gone"
        >
        <TextView
            android:id="@+id/movoTextFailed"
            android:layout_width="wrap_content"
            android:layout_height="wrap_content"
            android:layout_alignParentTop="true"
            android:layout_centerHorizontal="true"
            android:gravity="center"
            android:textSize="20sp"
            android:text="Sync failed. Please try again."/>
        <ImageView
            android:id="@+id/movoPictureFailed"
            android:layout_centerHorizontal="true"
            android:layout_below="@+id/movoTextFailed"
            android:layout_width="140dip"
            android:layout_height="200dip"
            android:src="@drawable/challenges_sel"/>

    </RelativeLayout>

    <Button
        android:layout_width="wrap_content"
        android:layout_height="wrap_content"
        android:text="Ok"
        android:id="@+id/btnOk"
        android:layout_alignParentBottom="true"
        android:layout_centerHorizontal="true"
        android:layout_marginBottom="30dp" />


</RelativeLayout>